# Environment
ENVIRONMENT=production

# Database (será preenchido automaticamente pelo Railway/Heroku)
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Configuration
SECRET_KEY=sua-chave-super-secreta-de-producao-aqui-mude-isso
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_HOURS=24

# Application Configuration
DEBUG=False
HOST=0.0.0.0

# CORS Configuration
ALLOWED_ORIGINS=https://espacoviv.com,https://www.espacoviv.com

# Email Configuration (opcional - para recuperação de senha)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=contato@espacoviv.com
SMTP_PASSWORD=sua-senha-de-app-do-gmail